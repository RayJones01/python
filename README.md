# Python Projects + Notes

This repo contains a collection of my personal pet projects (programs and scripts) and notes

```python
def greet(planet):
  return f"Hello, {planet}!"

print(greet("world"))
```
